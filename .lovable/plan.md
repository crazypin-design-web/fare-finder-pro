# Flight Price Notifier v1

## Build
- Replace the placeholder with a polished, mobile-responsive dark landing page using the requested violet accents, bilingual copy, three exact feature cards, and footer.
- Add separate email/password sign-in and sign-up screens with clear loading, validation, and error states.
- Add a protected `/app` page that greets the signed-in user by email, shows the requested milestone message, and supports safe sign-out.

## Authentication
- Use Lovable Cloud authentication only; do not create profiles, subscriptions, or other custom tables.
- Enable email/password login and disable confirmation so successful registration can immediately continue to `/app`.
- Keep authentication state synchronized so signed-in users see the app link and signed-out users return to sign-in.

## Design and validation
- Establish semantic near-black, violet, and supporting accent tokens with Inter typography and subtle reduced-motion-safe animation.
- Add unique metadata for each public page.
- Verify the landing page on desktop and mobile, plus sign-up, sign-in, protected access, and sign-out behavior.
